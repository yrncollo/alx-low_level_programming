#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char hello[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", hello);
	return (0);
}
